<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Notifikasi Pemesanan</title>
  <link rel="stylesheet" href="/stylesheets/notifikasi.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
  <!-- Navbar Partial -->
  <%- include('partials/navbar') %>

  <div class="wrapper d-flex flex-column min-vh-100">
    <div class="container my-5">
      <h2 class="mb-4 text-center fw-bold">🔔 Notifikasi Pemesanan</h2>

      <!-- Flash Messages -->
      <% if (error && error.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= error %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% } %>

      <% if (success && success.length > 0) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= success %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% } %>

      <% if (user) { %>
        <div class="mb-3 text-end">
          <span class="badge bg-primary">👤 <%= user.username %></span>
        </div>
      <% } %>

      <% if (!user) { %>
        <div class="alert alert-warning text-center" role="alert">
          🔒 Silakan login terlebih dahulu untuk melihat notifikasi.
        </div>
      <% } else if (!notifikasi || notifikasi.length === 0) { %>
        <div class="alert alert-info text-center" role="alert">
          Tidak ada notifikasi yang tersedia.
        </div>
      <% } else { %>
        <div class="row g-3">
          <% notifikasi.forEach((n, i) => { %>
            <div class="col-md-6 col-lg-4">
              <div class="card shadow-sm h-100">
                <div class="card-body">
                  <h5 class="card-title">
                    <span class="badge 
                      <%= n.status_pemesanan === 'Disetujui' ? 'bg-success' : 
                          n.status_pemesanan === 'Ditolak' ? 'bg-danger' : 
                          'bg-warning text-dark' %>">
                      <%= n.status_pemesanan %>
                    </span>
                  </h5>
                  <p class="card-text mt-2"><%= n.isi_pesan %></p>
                </div>
                <div class="card-footer text-muted small">
                  Dikirim: <%= new Date(n.waktu_kirim).toLocaleString() %>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      <% } %>
    </div>

    <!-- Footer Partial -->
    <%- include('partials/footer') %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
